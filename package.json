{
  "name": "pregnova-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:web": "node --test test/web_dashboard/*.mjs",
    "test:security": "firebase emulators:exec --only firestore \"node --test test/security/firestore_rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.24.2"
  }
}

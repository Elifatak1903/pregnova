import 'package:flutter/material.dart';

class OlcumGecmisiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Ölçüm Geçmişi")),
      body: Center(child: Text("Buraya ölçüm geçmişi listelenecek")),
    );
  }
}